{
  "projectSettings": {
    "framework": "vite",
    "buildCommand": "npm run build",
    "outputDirectory": "dist"
  },
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "env": {
    "NEXT_PUBLIC_API_URL": "@api_url"
  }
}
